// Legacy script.js file - functionality moved to js/main.js
// This file is kept for compatibility with the original template structure

// Global variables
let visitorCount = 0;

// DOM Content Loaded Event
document.addEventListener('DOMContentLoaded', function() {
    console.log('Legacy script.js loaded - Azure integrations handled by js/main.js');
});

// Export functions for potential use in other modules
if (typeof module !== 'undefined' && module.exports) {
    module.exports = {
        // Legacy exports
    };
}
